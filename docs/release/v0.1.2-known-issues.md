# MaTE X v0.1.2 Known Issues

**Candidate:** `v0.1.2-rc.2`  
**Source commit (build):** `aa2ed86827aad780b9e375ff4cf2e6de22d3261d`

## Blocking for public release

| ID | Severity | Issue | Impact | Mitigation |
| --- | --- | --- | --- | --- |
| — | — | **None remaining after founder scope decision** | — | Signing/notarization intentionally **out of scope** for v0.1.2 |

## Founder-accepted / out of scope for v0.1.2

| ID | Severity | Issue | Notes |
| --- | --- | --- | --- |
| REL-001 | Out of scope | Apple codesign identity / notarization credentials | **Not required for this release.** Forge signs only when `APPLE_ID` is set; unsigned DMG/ZIP are the authorized distribution form for v0.1.2. |
| REL-002 | Out of scope | Unsigned app / Gatekeeper rejection | Expected for this cut. Users may need to right-click → Open or allow in System Settings on first launch. |
| REL-003 | P2 accepted | Interactive founder GUI dogfood of all four modes on installed DMG | Automated policy/journey suite PASS (36). Full live provider dogfood remains recommended but is not a merge blocker after founder scope decision. |
| REL-004 | Closed | PR #9 branch lag | Resolved: PR head fast-forwarded and pushed to final remote SHA. |

## Accepted / non-blocking for macOS RC

| ID | Severity | Issue | Notes |
| --- | --- | --- | --- |
| REL-010 | P2 | Windows qualification deferred | Intentional for v0.1.2; Windows CI not a merge blocker |
| REL-011 | P2 | Workspace trust still exposes an **Unrestricted** autonomy setting that can widen tool policy beyond Auto’s conversational framing | Distinct from behavior-mode Auto (`auto_scoped`); GitGate still hard-gates commit/push. Founder may accept for v0.1.2 or schedule a follow-up to renames/labels |
| REL-012 | P2 | Legacy `AssistantAccess` value `"full"` remains in contracts for trusted/legacy paths | Auto behavior mapping uses `"scoped"` / `"approval"` via `behaviorRunOptions`; `"full"` is not the Auto product mode |
| REL-013 | P2 | README historically described Factory Mode Lite | Release docs and README for v0.1.2 reframe around EngineeringTask + behavior modes |
| REL-014 | P2 | Co-located unit tests live primarily under `src/**/*.test.ts` while AGENTS directory map also mentions `tests/` | `qa/` holds packaged qualification; `tests/helpers` holds fake adapters outside production |
| REL-015 | P3 | EGL/GPU log noise on headless-ish launch (`eglQueryDeviceAttribEXT: Bad attribute`) | Did not prevent BrowserWindow/renderer boot in clean install smoke |
| REL-016 | P3 | macOS package built on **x86_64** host (`darwin-x64`) | Apple Silicon users should re-package on arm64 host for native arm64 artifacts when available |

## Explicitly not bugs

- Git commit/push remaining blocked without fresh proof is **required behavior**.
- Review mode rejecting mutations is **required behavior**.
- Auto refusing unrestricted Full access is **required behavior**.
- Spec Kit is **not** a product dependency or user-facing feature.
